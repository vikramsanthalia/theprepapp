// Generated by ToffeeScript 1.6.3-1
(function() {
  var assert, t;

  assert = require('assert');

  t = require('../lib/timed.js');

  function delay(ms, func) {
    return setTimeout(func, ms);
  };

  describe('timed', function() {
    return describe('reset', function() {
      it('should reset the timer to 0', function() {
        var was;
        t.reset();
        was = t.rounded();
        console.log(was);
        return assert(was < 0.09);
      });
      return it('should return time in ms since reset', function(done) {
        var elapsed,
          _this = this;
        t.reset();
        delay(700, function() {
          elapsed = t.rounded();
          console.log("Elapsed: " + elapsed);
          assert.ok(elapsed > 699.99999);
          assert.ok(elapsed < 705.001);
          return done();
        });
      });
    });
  });

}).call(this);
